<?php

function users_list_theme($existing, $type, $theme, $path) {
    return [
        'users_list_search' => [ // ← Coincide con `#theme => 'users_list_search'`
            'variables' => [],
            'template' => 'users_list_search',
        ],
    ];
}

